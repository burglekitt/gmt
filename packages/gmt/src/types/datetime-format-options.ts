/**
 * Options accepted by date/time formatting functions that delegate to
 * `Intl.DateTimeFormat` (via Temporal's `.toLocaleString()` or
 * `TemporalIntl.DateTimeFormat`).
 */
export type DateTimeFormatOptions = Intl.DateTimeFormatOptions;
