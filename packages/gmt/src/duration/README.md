# Duration API

Parsing and validation for ISO 8601 duration strings (e.g. `"P1DT2H30M"`). All functions accept and return ISO 8601 duration strings.

## Modules

### parse

Parse and normalize:

- `parseDuration`

### validate

Validation helpers:

- `isValidDuration`
