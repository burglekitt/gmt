---
"@northguild/gmt": patch
---

Add JSDoc to `formatUtc` and `formatUnix`, and to internal helpers `startOrEndOfUtc` and `startOrEndOfUnix`. Replace the six namespace `README.md` files with one-line stubs pointing at the docs site reference section.
